public class Solution {
    public int lengthOfLastWord(String s) {
        String[] tmp = s.split(" ");
        if (tmp.length == 0)
            return 0;
        String last = tmp[tmp.length-1];
        return last.length();
    }
}
